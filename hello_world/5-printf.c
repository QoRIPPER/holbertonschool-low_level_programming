#include <stdio.h>
/**
 * main - prints text
 * Description: prints texts exactly
 * Return: always 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art, \n");
return (0);
}
