#include <stdlib.h>
#include <time.h>
/**
* main - prints code
* Description: prints more code
* Return: always 0
*/
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	
	return (0);
}
